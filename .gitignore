# Data — large, redistributable from Kaggle, never committed
data/
archive.zip
*.csv

# Python
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.coverage
htmlcov/

# Scratch file the rollback runbook generates for the curl loop
payload.json

# Env / editor
.env
.DS_Store
.idea/
.vscode/

# Terraform (Phase 3)
.terraform/
*.tfstate
*.tfstate.*
crash.log
tfplan
*.tfvars
# ...except the committed environment files: they hold no secrets and they are
# how a second environment is added without a refactor.
!infra/envs/*.tfvars
# Generated by scripts/bootstrap-state.sh. Machine-specific -- the state bucket
# name embeds an account id.
infra/backend.hcl
.claude/settings.local.json